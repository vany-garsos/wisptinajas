---
import Title from "./Title.astro";

interface Props {
    id: string;
    title?: string;
    className?: string;
    titleClassName?: string;
}

const { id, title, className, titleClassName } = Astro.props;
---

<section
    id={id}
    class:list={[
        "py-28 px-6 max-w-7xl mx-auto flex flex-col items-center justify-center",
        className,
    ]}
>
    {
        title && (
            <Title level={2} class={titleClassName}>
                {title}
            </Title>
        )
    }
    <slot />
</section>
