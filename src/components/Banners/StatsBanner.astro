---
import { Icon } from "astro-icon/components";
---

<section class="relative z-20 px-6">
  <div class="max-w-5xl mx-auto">
    <div
      class="bg-gray-900/40 backdrop-blur-xl rounded-3xl p-8 md:p-8 flex flex-wrap justify-center md:justify-between items-center gap-8 md:gap-4"
    >
      <!-- Stats Item 1 -->
      <div class="flex flex-col items-center text-center px-4 group">
        <div
          class="flex items-center gap-3 mb-2 transition-transform duration-300 group-hover:scale-105"
        >
          <Icon name="lucide:wifi" class="size-14 text-primary" />
        </div>
        <span
          class="text-gray-400 font-medium uppercase tracking-wider text-xs md:text-sm"
        >
          Conectividad a tu alcance
        </span>
      </div>

      <!-- Divider (md+) -->
      <div class="hidden md:block h-12 w-px bg-white/10"></div>

      <!-- Stats Item 2 -->
      <div class="flex flex-col items-center text-center px-4 group">
        <div
          class="flex items-center gap-3 mb-2 transition-transform duration-300 group-hover:scale-105"
        >
          <Icon name="lucide:home" class="size-14 text-primary" />
        </div>
        <span
          class="text-gray-400 font-medium uppercase tracking-wider text-xs md:text-sm"
        >
          Conectando a nuestras comunidades
        </span>
      </div>

      <!-- Divider (md+) -->
      <div class="hidden md:block h-12 w-px bg-white/10"></div>

      <!-- Stats Item 3 -->
      <div class="flex flex-col items-center text-center px-4 group">
        <div
          class="flex items-center gap-3 mb-2 transition-transform duration-300 group-hover:scale-105"
        >
          <span class="text-4xl md:text-5xl font-extrabold text-primary">
            <Icon name="lucide:user-cog" class="size-14 text-[#647EF5]" />
          </span>
        </div>
        <span
          class="text-gray-400 font-medium uppercase tracking-wider text-xs md:text-sm"
        >
          Atención y soporte técnico
        </span>
      </div>
    </div>
  </div>
</section>

<style>
  .text-primary {
    color: #647ef5;
  }
</style>
